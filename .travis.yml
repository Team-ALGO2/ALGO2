jobs:
  include:
    - stage: make
      name: C Compiling
      language: C
      install: 
        - make install_deps
      script:
        - make create_dir compile
    - stage: test
      name: C Tests
      script:
         - cd bin
         - ./unittest
